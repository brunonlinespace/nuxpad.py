# Nuxpad 2 source-portable format

Linux: `./portable/run-nuxpad.sh`

Windows CMD: `portable\run-nuxpad.cmd`

Windows PowerShell: `.\portable\run-nuxpad.ps1`

Requires host Python 3 and PyQt6; uses the host Qt theme.
