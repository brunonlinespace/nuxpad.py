# Windows x64 portable build

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\windows\build-windows-portable.ps1
```

Outputs the `Nuxpad-2.0.0-rc5.1-Windows-x64-portable` folder/ZIP and checksum/build records. It does not install file associations or write application registry entries.
