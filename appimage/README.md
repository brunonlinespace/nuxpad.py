# Fedora 43/44 AppImage build

```bash
chmod +x appimage/build-fedora-appimage.sh
./appimage/build-fedora-appimage.sh
```

Outputs `dist/Nuxpad-2.0.0-rc5.1-x86_64.AppImage` plus SHA-256, build-info, and Python lock files.
